name: Daily Update Fund Data

on:
  # Schedule: 19:30 Beijing time (11:30 UTC), Monday-Friday only — the ONLY automatic data update
  # A-share market closes at 15:00, data is available after ~19:00
  # Note: GitHub scheduled runs may be delayed under load; weekend/holiday runs are skipped
  schedule:
    - cron: '30 11 * * 1-5'
  # Code pushes only rebuild & deploy the site; they do NOT re-fetch data
  push:
    branches: [main]
  # Manual trigger via GitHub web interface (fetches data + deploys)
  workflow_dispatch:

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  update:
    name: Fetch Data & Build
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Python
        if: github.event_name != 'push'
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install Python dependencies
        if: github.event_name != 'push'
        run: |
          pip install tushare pandas requests

      - name: Fetch data from Tushare
        if: github.event_name != 'push'
        env:
          TUSHARE_TOKEN: ${{ secrets.TUSHARE_TOKEN }}
        run: python scripts/update_data.py

      - name: Copy updated data to public
        if: github.event_name != 'push'
        run: cp src/data/fund_data.json public/fund_data.json

      - name: Commit updated data back to repo
        if: github.event_name != 'push'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/data/fund_data.json scripts/cache/sector_history.json scripts/cache/margin_history.json
          git diff --cached --quiet || git commit -m "data: 自动更新资金数据 $(date +'%Y-%m-%d')"
          git push || true

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install npm dependencies
        run: npm ci

      - name: Build project
        run: npm run build

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: './dist'

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
